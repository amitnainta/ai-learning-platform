---
description: Get a full narrative status report across every in-flight and recently shipped factory work item.
---

Invoke the `factory-manager` subagent to produce a full project status report per its own instructions. Show me the report directly in this conversation, and confirm the path it saved a copy to under `factory/reports/`.
