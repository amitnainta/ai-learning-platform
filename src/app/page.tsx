// Placeholder home page — proves the scaffold has a renderable route and
// gives CI/Lighthouse a perf/smoke target (AC1, AC11). Real product UI
// arrives with later feature work items.
export default function HomePage() {
  return (
    <main>
      <h1>AI Learning Platform</h1>
      <p>Scaffolding placeholder — the real home page ships with later work.</p>
    </main>
  );
}
